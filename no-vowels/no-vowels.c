#include <cs50.h>
#include <stdio.h>
string replace(string s[]);

int main(int argc, string argv[])
{
    if(argc == 1)
    {
        printf("Fuck You\n");
        return 1;
    }
    else
    {
        printf("%s\n",replace(argv))
    }
}



string replace(string s[])
{
    s[1]
}